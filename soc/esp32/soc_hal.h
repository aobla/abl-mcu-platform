#ifndef SOC_HAL_H
#define SOC_HAL_H

/* Vendor header for this SoC. Substituted through the SoC definition include
 * path, so common code does not need to know the family. */
#include <driver/gpio.h>

#endif /* SOC_HAL_H */
