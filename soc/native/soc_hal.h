#ifndef SOC_HAL_H
#define SOC_HAL_H

/* Native (host) has no vendor header: the SoC shim only pulls in the standard
 * fixed-width types used by the generated pin code. */
#include <stdint.h>
#include <stdbool.h>

#endif /* SOC_HAL_H */
